// Placeholder for NextAuth - using simple auth in MVP
// This will be properly configured when NextAuth v5 is more stable

export const GET = (req: any) => new Response('Auth endpoint', { status: 200 });
export const POST = (req: any) => new Response('Auth endpoint', { status: 200 });
